# Enemy refactor — photographie initiale, non autoritaire

> Pour la composition et le commandement **V2 actuels**, commencer par [README.md](README.md). Ce document conserve le contexte de sa passe et ne remplace pas les nouveaux contrats.

> Document historique de phase 0. Il ne décrit pas le runtime final; voir `FINAL_ENEMY_OPTIMIZATION_REPORT.md` et `REGRESSION_MATRIX.md` pour l'état autoritaire.

Updated: 2026-08-25

- Phase: 2 — canonical foundation active; typed spawn/archetype data validated; first scene-scoped registry consumer validated in `combat_lab`; performance foundation and isolated registry cost captured; action-state priority characterized; no runtime action FSM is integrated.
- Completed: every runtime construction route reaches the sole typed factory construction point. Battles 01–03, `main.gd`, and the procedural wave director construct `HopliteEnemySpawnRequest` directly; only the concurrently edited Forge runtime still uses the documented dictionary adapter. A derived non-shared typed archetype view covers all 22 profiles. The real action priority is characterized; Pilot A failed dispersion and was fully rolled back. Faction/target/claim/friendly-fire/live-free behavior is protected. The ordered, event-maintained, non-owning-ID registry passes synthetic and real factory lifecycle tests. After the noisy full-spawn gate was rejected, a predeclared isolated 15-process gate passed (`324 us` median cold register-56 p50, `380 us` p95, `19 us` IQR). `combat_lab` is the sole controlled scene consumer with exact 99-enemy parity and teardown. Read-only crowd parity proves spawn-time group/current-AI divergence, so no gameplay system queries the registry and all other production requests remain null.
- Files modified by this refactor: nine files under `docs/enemy_refactor/`; `scripts/enemy/enemy_{factory,spawn_request,archetype_data,archetypes,combatant_registry}.gd`; the isolated `_exit_tree()` claim cleanup in `scripts/enemy/athenian_enemy.gd` (concurrent patrol edits in that file are user-owned); `scripts/battle/battle_01.gd`; `scripts/main.gd`; `scripts/campaign/procedural_wave_director.gd`; targeted Battle/factory/archetype/action/faction/registry/procedural/laboratory/performance probes including `enemy_registry_operation_benchmark.gd`; raw logs under `.tmp_tools/enemy_refactor/`.
- Tests passed: Battle spawn and full valid dictionary-option matrix; factory route guard; standalone registry contract/order/liveness/weak teardown; optional real factory registration; 15-process isolated registry gate; exact 99-enemy laboratory registry parity/teardown and 56-unit/21-shield composition; real procedural soldier/miniboss/boss spawn contracts; typed data equality/IDs/alias/fallback/deep-copy for 22 profiles; faction groups, distinct/null targets, claims/retaliation/friendly fire/live-free cleanup; action-state priority/fallthrough; roster, crowd, archer, phalanx equipment, enemy combat; 14 specialized animation openers; 13 grounded pose fixtures; giant floor/head/crawl/wall-run/anatomy; combat attack/cinematic paths; narrative battle; Last Flame; Battles 01/02/03 startup; v2 performance matrix. Pilot A's integration gate remained inconclusive and rejected. The final laboratory process exits successfully; a verbose forced-quit run separately reports the pre-existing music stream `Ripped Crown Run.mp3` still referenced at engine shutdown.
- Baseline failures: no unresolved enemy-refactor failure. The intentional live-free red run reproduced a stale claim before D-013; the final probe and adjacent suites pass. Windows root-certificate-store warning is known environment noise. The laboratory shutdown's two leaked ObjectDB instances are `AudioStreamPlaybackMP3`/`AudioStreamMP3` for `Ripped Crown Run.mp3`, not enemies or registry entries; this is recorded as a separate audio-lifecycle debt and is not treated as proof of complete process-wide leak freedom.
- Blockers: no architecture blocker. Direct Forge adapter migration is deferred while `world_runtime.gd` is concurrently user-modified; it remains inside the canonical factory route.
- Next exact action: keep registry authority frozen at `combat_lab`; characterize a spawn-participation registry view that preserves `combatant_ai` order plus the cleanup player union, without changing crowd. Separately extend per-zone detached-limb anatomy coverage. Do not migrate targeting or action-FSM authority without full priority/claim/tie and performance gates.
